# このリポジトリで作業するときの規約

## このリポジトリの目的

XML 計画ファイル編集ツールの、**画面仕様と I/F 仕様を保守すること**。

サーバ側（バックエンド）は実装済みだが、**その仕様がまだ入手できていない。**
そのため現在の仕様は**ほぼすべて推測**であり、
後日サーバ側の実物と突き合わせて差し替えることが前提になっている。

**この「差し替えやすさ」が、このリポジトリの最重要の設計目標である。**

---

## 文章を書くときの規約

### 1. 略語を説明なしに使わない

`FE` `BE` のような略語は本文で使わない。「画面側」「サーバ側」と書く。

読み手には、この分野に不慣れな人も、途中から参加する人も含まれる。
**読んでいて立ち止まらせないこと**を優先する。

初出の専門用語は [`docs/00-glossary.md`](docs/00-glossary.md) に定義を置き、
本文からはそこを参照する。

### 2. 「なぜ」を書く

「こうする」だけでなく「なぜそうするのか」を書く。

後から仕様を変える人は、理由が分からないと**変えてよいのか判断できない。**
理由が書いてあれば、前提が変わったときに自分で判断できる。

### 3. 推測は推測と明示する

サーバ側が未確認である以上、書いてあることの多くは想定に過ぎない。
**確定した事実と想定を混ぜて書かない。**

確定度マーカーを付ける:

| 印 | 意味 |
|:---:|---|
| 🟢 | 確定 — サーバ側の実装を確認済み |
| 🟡 | 暫定 — 間接的に聞いた |
| 🔴 | 推測 — こちらの想定。要確認 |

🔴 には確認事項 ID（`Q-001` 形式）を添え、
[`docs/90-open-questions.md`](docs/90-open-questions.md) に項目を作る。

> **凡例や手順の説明でマーカーに言及する行**には、行末に `<!--legend-->` を付ける。
> Markdown では表示されず、集計スクリプトが実際の使用箇所と区別できるようになる。

### 4. 手で数えた数字を書かない

「確認事項は 25 件」のような数字をドキュメントに書かない。**必ず古くなる。**
集計は `scripts/check-spec.sh` に任せ、本文からはそれを指す。

---

## 仕様を変えるときの手順

### API を変える場合 — 3 点セットで直す

1. [`docs/interface/openapi.yaml`](docs/interface/openapi.yaml) — **これが正**
2. [`docs/interface/21-api.md`](docs/interface/21-api.md) — 解説版の辻褄を合わせる
3. [`docs/CHANGELOG.md`](docs/CHANGELOG.md) — いつ・何を・なぜ

> **どれか 1 つだけ直さない。**
> 食い違うと、以降の読み手が「どちらが正しいのか」を判断できなくなる。

### 画面仕様を変える場合

1. 該当する `docs/ui/*.md`
2. **操作 ID を新設したら [`docs/ui/10-screens.md`](docs/ui/10-screens.md) の索引にも追加する**
3. その操作が新しい API を必要とするなら、API 側にも反映する
4. [`docs/CHANGELOG.md`](docs/CHANGELOG.md) に記録

### 確認が取れた場合

1. 該当する記述を実物に合わせる
2. 確定度マーカーを 🔴 から 🟢 へ
3. **`docs/90-open-questions.md` の該当項目に結論を追記する。項目は消さない**
   — 判断の経緯が残ることに価値がある
4. [`docs/CHANGELOG.md`](docs/CHANGELOG.md) に記録

### 変更後は必ず実行する

```sh
bash scripts/check-spec.sh
```

---

## 二層構造を崩さない

| 層 | 何を決めるか | 正 |
|---|---|---|
| API の表面 | どこに何を送ると何が返るか | `docs/interface/openapi.yaml` |
| データの中身 | 計画ファイルの要素・属性 | `docs/interface/20-data-model.md` |

**この分離が、合わせ込みを機械作業にする。**

「API の形が違った」のか「XML の中身が違った」のかで直す場所が分かれ、
片方の修正がもう片方を壊さない。**混ぜて書かないこと。**

同じ理由で、**XML の要素名を画面仕様のあちこちに散らばらせない。**
要素名の知識はデータモデルの章に集約する。

---

## 画面仕様が API 仕様より先

**画面 → 必要な操作 → 必要な API** の順で導く。逆をやらない。

どんな画面があり何ができるかが決まらないと、必要な API は決められない。

各 API には「どの画面操作のために存在するか」を書く
（`openapi.yaml` では `x-ui-operations`）。
これにより、実物と違ったときに**どの画面が影響を受けるか**が分かる。

---

## モックについて

[`mock/`](mock/) は**ビルド不要・依存ゼロ**を保つ。

社内のパソコンに開発環境がなくても、
ファイルをダブルクリックすれば開けることに価値がある。

- 外部のライブラリを読み込まない
- ビルド手順を増やさない
- 図は SVG で直接描く

**モックは画面の形を確かめるためのものであり、実装の原型ではない。**
本実装が始まったら、モックは仕様の付属物として残す。

---

## やらないこと

- **技術スタックを勝手に決めない** — 未定。仕様が固まってから決める
- **推測を確定のように書かない**
- **確認事項の項目を消さない** — 結論を追記する
- **画面側の実装コードをまだ書かない** — 仕様とモックの段階
