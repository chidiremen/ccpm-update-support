# 画面一覧と操作の索引

> 用語は [`../00-glossary.md`](../00-glossary.md) で定義しています。

## この章の位置づけ

**画面仕様は API 仕様より先に決めます。**

どんな画面があり、そこで何ができるかが決まらないと、必要な API は決められないためです。
たとえば「タスクを 1 件だけ直す画面」があるなら部分更新の API が要りますが、
「XML を丸ごと差し替える画面」しかないなら全文置換だけで足ります。

そのため、**すべての操作に ID を振り**（例 `UI-GANTT-06`）、
API 仕様の側から「この API はどの操作のために存在するか」を参照します。

こうしておくと、後日サーバ側の実物が想定と違っていたときに
**どの画面が影響を受けるかが即座に分かります。** 逆に、どの操作からも参照されない
API は不要だと分かります。

## 画面の一覧

| 画面 | 役割 | 詳細 |
|---|---|---|
| ファイルを開く | 計画ファイルを選び、読み込む | この章の下部 |
| ガントチャート | いつ何をやるかを時間軸で見る | [`11-gantt.md`](11-gantt.md) |
| ネットワーク図 | タスクのつながりとボトルネックを見る | [`12-network.md`](12-network.md) |
| 変更内容の確認 | どこを変えたか確かめて保存する | [`13-diff.md`](13-diff.md) |

画面のつながりは [`14-navigation.md`](14-navigation.md) を参照してください。

**ガントチャートとネットワーク図は、同じ計画を別の角度から見たものです。**
どちらでタスクを選んでも、もう片方でも同じタスクが選択された状態になります。

## 操作 ID の索引

各操作の詳しい仕様は、リンク先の章にあります。

### 共通（すべての画面）

| ID | 操作 | 確定度 |
|---|---|:---:|
| `UI-COMMON-01` | ガント／ネットワーク図／変更確認を切り替える | 🔴 |
| `UI-COMMON-02` | 未保存の変更があるまま閉じようとしたら警告する | 🔴 |
| `UI-COMMON-03` | サーバ側のエラーを利用者に伝える | 🔴 |
| `UI-COMMON-04` | 選択中のタスクを画面間で引き継ぐ | 🔴 |

### ファイルを開く

| ID | 操作 | 確定度 |
|---|---|:---:|
| `UI-OPEN-01` | 計画ファイルを指定して開く | 🔴 (`Q-004`) |
| `UI-OPEN-02` | 最近開いたファイルの一覧から開く | 🔴 |
| `UI-OPEN-03` | 開けなかった理由を表示する | 🔴 (`Q-014`) |

### ガントチャート → [`11-gantt.md`](11-gantt.md)

| ID | 操作 | 確定度 |
|---|---|:---:|
| `UI-GANTT-01` | タスクを階層構造で一覧表示する | 🔴 (`Q-008`) |
| `UI-GANTT-02` | 各タスクの期間を帯で表示する | 🔴 (`Q-016`) |
| `UI-GANTT-03` | クリティカルチェーンを強調表示する | 🔴 (`Q-011`) |
| `UI-GANTT-04` | バッファを表示する | 🔴 (`Q-010`) |
| `UI-GANTT-05` | タスクを選び、詳細を表示する | 🔴 |
| `UI-GANTT-06` | タスクの値を編集する | 🔴 (`Q-006`) |
| `UI-GANTT-07` | 表示する期間の範囲を変える | 🔴 |
| `UI-GANTT-08` | 依存関係を線で表示する | 🔴 (`Q-009`) |

### ネットワーク図 → [`12-network.md`](12-network.md)

| ID | 操作 | 確定度 |
|---|---|:---:|
| `UI-NET-01` | タスクをノード、依存関係を矢印で表示する | 🔴 (`Q-021`) |
| `UI-NET-02` | クリティカルチェーンを強調表示する | 🔴 (`Q-011`) |
| `UI-NET-03` | ノードを選び、詳細を表示する | 🔴 |
| `UI-NET-04` | 拡大・縮小・移動する | 🔴 |
| `UI-NET-05` | ノードを自動配置する | 🔴 (`Q-020`) |
| `UI-NET-06` | 大きな計画でも扱えるようにする | 🔴 (`Q-012`) |

### 変更内容の確認 → [`13-diff.md`](13-diff.md)

| ID | 操作 | 確定度 |
|---|---|:---:|
| `UI-DIFF-01` | 変更点を一覧で表示する | 🔴 (`Q-013`) |
| `UI-DIFF-02` | 保存前に計画を検証する | 🔴 (`Q-014`) |
| `UI-DIFF-03` | 計画ファイルに保存する | 🔴 (`Q-015`) |
| `UI-DIFF-04` | XML の文字レベルの差分を見る | 🔴 |
| `UI-DIFF-05` | 個々の変更を取り消す | 🔴 |
| `UI-DIFF-06` | 保存が衝突したときに復帰する | 🔴 (`Q-005`) |

---

## ファイルを開く画面

### 画面の構成

```
┌──────────────────────────────────────────┐
│  計画ファイルを開く                        │
├──────────────────────────────────────────┤
│                                          │
│   ┌──────────────────────────────────┐   │
│   │  ここにファイルをドロップ           │   │
│   │       または                      │   │
│   │    [ ファイルを選ぶ ]              │   │
│   └──────────────────────────────────┘   │
│                                          │
│   最近開いたファイル                       │
│   ・ project-a.xml     2 時間前           │
│   ・ project-b.xml     昨日               │
│                                          │
└──────────────────────────────────────────┘
```

### `UI-OPEN-01` 計画ファイルを指定して開く 🔴推測

**ファイルの指定方法が 2 通り考えられ、どちらになるかはサーバ側次第です**（→ `Q-004`）。

| 方式 | 内容 | 向いている場合 |
|---|---|---|
| **パス指定** | 利用者がファイルの場所を入力し、サーバ側がそこを読む | サーバ側が利用者と同じパソコンで動く（＝今回の構成）。大きなファイルでも速い |
| **アップロード** | 画面側がファイルの中身を読み、サーバ側へ送る | サーバ側が別の場所にあってもよい。ブラウザの制約に素直 |

**今回の構成（同じパソコンの中）ではパス指定が自然**なので、
仕様の初版はパス指定で書いています。ただしブラウザは安全上の理由から
**利用者が選んだファイルの絶対パスを画面側に教えてくれません。**

したがって、パス指定にするなら以下のいずれかが必要です:

- 利用者にパスを文字で入力してもらう
- サーバ側が「開けるファイルの一覧」を返し、そこから選ばせる

⚠️ **この点はサーバ側の作りに強く依存します。最優先の確認事項です**（→ `Q-004`）。

### `UI-OPEN-02` 最近開いたファイルの一覧 🔴推測

一覧をどこに覚えておくかで 2 案あります。

- **画面側に覚える** — ブラウザの保存領域を使う。サーバ側の改修が不要
- **サーバ側に覚える** — 別のブラウザで開いても一覧が残る

サーバ側に該当機能がなければ画面側で持ちます。**初版は画面側**とします。

### `UI-OPEN-03` 開けなかった理由を表示する 🔴推測

失敗の種類ごとに、利用者が次に何をすればよいか分かる文言を出します。
エラーの種類は [`../interface/22-errors.md`](../interface/22-errors.md) を参照。

| 状況 | 利用者に見せる内容 |
|---|---|
| ファイルが見つからない | パスを確認するよう促す |
| XML として壊れている | 壊れている箇所（行・列）を示す |
| 形式が想定と違う | 対応している形式を示す |
| 読み取り権限がない | 権限を確認するよう促す |
